<p>毎日6：00動画更新中</p>
<video src="/movie/img_3124.mp4"
       width="1000" height="500"
       autoplay muted playsinline >
</video>
<div class="contents_row">
  <% @cats.each do |cat| %>
    <div class="content_post" >
    <%= link_to cat_path(cat) do %>
      <div class="one_cat">
      <% if cat.images.count > 0 %>
        <%= image_tag cat.images.first, class:'cat-image' %>
          <% if cat.family.present? %>
           <div class='family-done'>
              <span>家族ができました!!</span>
          </div>
          <% end  %>
      <% end %>
      <%= cat.c_name %>
      </div>
    <% end %>  
    </div>
  <% end %>
</div>
      <div class="header__bar row">
        保護された方のページ
        <% if trader_signed_in? %>
          <div class="user_nav grid-6">
            <%= link_to "保護された方のマイページ", "/traders/#{current_trader.id}" %>
            <%= link_to "ログアウト", destroy_trader_session_path, method: :delete %>
        <% else %>
          <div class="grid-6">
            <%= link_to "ログイン", new_trader_session_path, class: "post" %>
            <%= link_to "新規登録", new_trader_registration_path, class: "post" %>
          </div>
        <% end %>
          </div>
      </div>
      <%= link_to "猫登録", new_cat_path %>
